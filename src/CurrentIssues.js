import React from 'react';

class CurrentIssues extends React.Component {
    render() {
        return(
            <h1>Display a list of issues the user is currently working on</h1>
        );
    }
}

export default CurrentIssues;