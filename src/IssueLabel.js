import React from 'react';

class IssueLabel extends React.Component {
    render() {
        return(
            <h1>Display issues that match given labels for a repo the user is interested in</h1>
        );
    }
}

export default IssueLabel;