import React from 'react';

class LanguageList extends React.Component {
    render() {
        return(
            <h1>Display a simple list of languages the user has used</h1>
        );
    }
}

export default LanguageList;